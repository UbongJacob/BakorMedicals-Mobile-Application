import { View, StyleSheet, Text } from "react-native";

const DoctorsCard = (): JSX.Element => {
  return (
    <View style={style.container}>
      <Text>DoctorsCard</Text>
    </View>
  );
};

const style = StyleSheet.create({
  container: {},
});

export default DoctorsCard;
